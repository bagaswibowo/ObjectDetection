[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 30 --log-level info wsgi:app"
healthcheckPath = "/health"
healthcheckTimeout = 60
startupTimeout = 60 